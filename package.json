{
  "name": "reuseable-backend",
  "version": "0.0.0",
  "private": true,
  "description": "A catalogue of reusable backend components (@reuseablebackend/*) for SilverRock products.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "start:api": "pnpm --filter @reuseablebackend/api start"
  }
}
