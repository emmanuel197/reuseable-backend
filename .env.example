# Documents required env keys. Copy to .env and fill real values (never commit .env).
# The @reuseablebackend/secrets env provider reads these from process.env.

# Example secret keys (Wave-1). Add real keys as components need them.
DATABASE_URL=
JWT_SECRET=
